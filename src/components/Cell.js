import React from 'react';
import styled from 'styled-components';

const Cell = (props) => {
    return(
        <Test>
            sobaka
        </Test>
    )
}
const Test = styled.div`
    display: block;
`;
export default Cell;